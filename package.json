{
  "name": "state-tax-updater",
  "version": "2.0.0",
  "description": "Multi-state tax table updater with deterministic PDF extraction",
  "main": "main.js",
  "scripts": {
    "test": "node --test",
    "start": "electron .",
    "build": "electron-builder"
  },
  "build": {
    "appId": "com.hrblock.state-tax-updater",
    "productName": "State Tax Table Updater",
    "win": {
      "target": "nsis",
      "artifactName": "State-Tax-Table-Updater-Setup-${version}.${ext}"
    },
    "files": [
      "main.js",
      "preload.js",
      "renderer.js",
      "index.html",
      "styles.css",
      "states/**",
      "assets/**"
    ],
    "nsis": {
      "shortcutName": "State Tax Table Updater",
      "uninstallDisplayName": "State Tax Table Updater",
      "artifactName": "State-Tax-Table-Updater-Setup-${version}.${ext}"
    },
    "executableName": "StateTaxTableUpdater"
  },
  "dependencies": {
    "pdfjs-dist": "^4.8.69"
  },
  "devDependencies": {
    "electron-builder": "^24.0.0",
    "electron": "^28.0.0"
  },
  "author": "H&R Block"
}
